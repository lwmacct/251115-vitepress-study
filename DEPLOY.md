# 部署到 GitHub Pages 指南

本文档将指导你一步步将 VitePress 站点部署到 GitHub Pages。

## 前置要求

- Git 已安装
- 有 GitHub 账号
- 项目已推送到 GitHub 仓库

## 部署步骤

### 步骤 1: 确认配置

确保 `docs/.vitepress/config.ts` 中已经配置了正确的 `base` 路径：

```typescript
export default defineConfig({
  // 使用你的仓库名称
  base: '/251115-vitepress-study/',
  // ...其他配置
})
```

**重要提示：** `base` 必须与你的 GitHub 仓库名称匹配！

### 步骤 2: 推送代码到 GitHub

确保所有代码已经推送到 GitHub 的 `main` 分支：

```bash
git add .
git commit -m "准备部署到 GitHub Pages"
git push origin main
```

### 步骤 3: 启用 GitHub Pages

1. 打开你的 GitHub 仓库页面
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages**
4. 在 **Source** (源) 下拉菜单中选择 **GitHub Actions**

![GitHub Pages设置](https://docs.github.com/assets/cb-47267/mw-1440/images/help/pages/publishing-source-drop-down.webp)

### 步骤 4: 触发部署

推送代码到 main 分支会自动触发 GitHub Actions 工作流：

```bash
# 任何推送到 main 分支的操作都会触发部署
git push origin main
```

或者手动触发工作流：

1. 进入仓库的 **Actions** 标签页
2. 选择 **Deploy VitePress site to Pages** 工作流
3. 点击 **Run workflow** 按钮
4. 选择 `main` 分支
5. 点击绿色的 **Run workflow** 按钮

### 步骤 5: 等待部署完成

1. 进入 **Actions** 标签页
2. 查看正在运行的工作流
3. 等待两个任务完成（build 和 deploy）
4. 看到绿色的 ✓ 表示部署成功

### 步骤 6: 访问你的站点

部署成功后，你的站点将发布到：

```
https://<username>.github.io/251115-vitepress-study/
```

例如：
```
https://lwmacct.github.io/251115-vitepress-study/
```

## 自动化工作流说明

我们已经在 `.github/workflows/deploy.yml` 中配置了自动化部署流程：

- **触发条件**: 推送到 `main` 分支时自动运行
- **构建过程**:
  1. 检出代码
  2. 安装 Node.js 20
  3. 安装依赖
  4. 构建 VitePress
  5. 上传构建产物
- **部署过程**: 自动部署到 GitHub Pages

## 故障排查

### 问题 1: 页面显示 404

**原因**: `base` 路径配置不正确

**解决方案**:
1. 检查 `docs/.vitepress/config.ts` 中的 `base` 配置
2. 确保 `base` 与仓库名称一致
3. 重新构建和部署

### 问题 2: Actions 失败

**可能原因**:
- GitHub Pages 没有启用
- 权限设置不正确
- 构建错误

**解决方案**:
1. 查看 Actions 日志找出具体错误
2. 确保 GitHub Pages 已启用并设置为 "GitHub Actions"
3. 检查仓库的 Actions 权限设置

### 问题 3: 样式或资源加载失败

**原因**: 资源路径不正确

**解决方案**:
- 使用 `withBase()` 函数处理资源路径
- 或将资源放在 `docs/public/` 目录下

### 问题 4: 构建失败

**常见原因**:
- Markdown 中使用了未转义的 Vue 模板语法
- 存在死链接

**解决方案**:
```bash
# 本地测试构建
npm run docs:build

# 查看详细错误信息
# 根据错误信息修复问题
```

## 更新站点

每次你想更新网站时，只需：

```bash
# 1. 编辑文档
# 2. 提交更改
git add .
git commit -m "更新文档"

# 3. 推送到 GitHub
git push origin main

# 4. GitHub Actions 会自动构建和部署
```

## 本地预览构建结果

在部署前，建议先在本地测试：

```bash
# 构建
npm run docs:build

# 预览
npm run docs:preview
```

访问 http://localhost:4173/251115-vitepress-study/ 查看效果。

## 高级配置

### 自定义域名

如果你有自己的域名：

1. 在 `docs/public/` 目录创建 `CNAME` 文件
2. 文件内容为你的域名（例如：`docs.example.com`）
3. 在域名提供商处配置 DNS 记录

### 禁用 Jekyll

GitHub Pages 默认使用 Jekyll 处理文件。添加 `.nojekyll` 文件禁用：

```bash
touch docs/public/.nojekyll
```

我们的配置已经通过 GitHub Actions 自动处理了这一点。

## 下一步

- 添加自定义域名
- 集成分析工具 (如 Google Analytics)
- 优化 SEO
- 添加评论系统

## 相关资源

- [VitePress 部署文档](https://vitepress.dev/guide/deploy)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

---

🎉 恭喜！你已经成功将 VitePress 站点部署到 GitHub Pages！
